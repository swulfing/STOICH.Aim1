library(tidyverse)


brazil <- read.csv("Data/ChemData_Brazil.csv")
#what are the dates? 
#is NO3 as N or NO3?
#there is no TP -- for consistency, should we use TP since htat is what is available everywhere else?
